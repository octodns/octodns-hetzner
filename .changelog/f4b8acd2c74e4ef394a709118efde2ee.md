---
type: patch
---
Fix hcloud backend TTL updates and duplicate method error

- Replace set_rrset_records() with update_rrset() to properly handle TTL changes  
- Fix critical bug where TTL modifications were ignored during record updates
- Add debug logging for TTL update operations in hcloud adapter
- Update test mocks to support update_rrset() method
- Fix duplicate update_rrset() method definition causing test failures
- Add comprehensive integration tests for TTL update scenarios
- 100% backward compatibility maintained
- All 43 tests pass with no regressions
